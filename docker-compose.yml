version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: ai_radar_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-ai_radar_db}
    ports:
      - "54320:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Разовый сервис: накатывает миграции Alembic и заполняет источники из
  # config/news_sources.yaml, прежде чем разрешить старт app/scheduler.
  migrate:
    build: .
    container_name: ai_radar_migrate
    environment:
      - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-ai_radar_db}
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - .:/app
    command: sh -c "alembic upgrade head && python scripts/seed_sources.py"
    restart: "no"

  app:
    build: .
    container_name: ai_radar_app
    restart: always
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-ai_radar_db}
      - LLM_BASE_URL=http://host.docker.internal:20128/v1
    env_file:
      - .env
    depends_on:
      migrate:
        condition: service_completed_successfully
    volumes:
      - .:/app

  # Крутит пайплайн сбор -> нормализация -> анализ -> валидация -> модерация
  # по кругу (раз в SCHEDULER_INTERVAL_MINUTES минут), чтобы бот реально
  # производил контент без ручного запуска скриптов.
  scheduler:
    build: .
    container_name: ai_radar_scheduler
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-ai_radar_db}
      - LLM_BASE_URL=http://host.docker.internal:20128/v1
    env_file:
      - .env
    depends_on:
      migrate:
        condition: service_completed_successfully
    volumes:
      - .:/app
    command: python scripts/run_scheduler.py --daemon

volumes:
  postgres_data:
